import { MatDialogConfig, MatSnackBarConfig } from '@angular/material';

/**
 * Default options for SnackBar
 */
export const matSnackBarConfig: MatSnackBarConfig = { duration: 5000 };
/**
 * Default options for confirmation Dialog modal
 */
export const matDialogConfig: MatDialogConfig = {};
